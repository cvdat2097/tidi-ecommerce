
const INITIAL_STATE = {
    Header: {
    },
    Footer: {
    }
}

const layoutReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default layoutReducer;
